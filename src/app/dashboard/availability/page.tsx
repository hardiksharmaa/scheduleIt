import AvailabilityClient from "./AvailabilityClient";

export const metadata = { title: "Availability — ScheduleIt" };

export default function AvailabilityPage() {
  return <AvailabilityClient />;
}
